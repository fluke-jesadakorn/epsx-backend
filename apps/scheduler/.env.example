# Scheduler Service Configuration
SCHEDULER_SERVICE_HOST=0.0.0.0
SCHEDULER_SERVICE_PORT=4500

# Financial Service Connection
FINANCIAL_SERVICE_HOST=financial-service
FINANCIAL_SERVICE_PORT=4500

# Optional: Cron Schedule Override (uses server timezone)
# Format: * * * * * (minute hour day month weekday)
# FINANCIAL_DATA_CRON="0 0 * * *"  # Default: Every day at midnight
# DATA_CLEANUP_CRON="0 0 * * 0"    # Default: Every Sunday at midnight
